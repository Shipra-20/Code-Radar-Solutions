#include <stdio.h>
int main(){
    int a,b;
    scanf("%d %d",&a,&b);
    int sum = a+b;
    int sub = a-b;
    int mul = a*b;
    int div = a/b;
    if(a b +){
        printf("%d",sum);
    }
    else if(a b -){
        printf("%d",sub);
    }
    else if(a b *){
        printf("%d",mul);
    }
    else if(a b /){
        printf("%d",div);
    }
    else{
        printf("error")
    }
    return 0;
}